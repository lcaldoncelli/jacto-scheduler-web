server:
  port: 8889

spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://localhost:8888
      username: config
      password: c0nf1g
      enabled: false
      fail-fast: true
      retry:
        max-attempts: 10
        initial-interval: 10000
        multiplier: 1.0
        max-interval: 10000
    gateway:
      discovery:
        locator:
          enabled: true
  output:
    ansi:
      enabled: always



eureka:
  client:
    registerWithEureka: true

springdoc:
  api-docs:
    path: /api-docs
